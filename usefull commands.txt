// this will deploy the contract to hardhat deafult network
yarn hardhat run scripts/deploy.js

//this will run it in ganache network // just make sure to place the private key in the .env file
yarn hardhat run scripts/deploy.js --network ganache


//deploy to localhost using hardhat network
//NOTE: run yarn hardhat node         //in another terminal first
yarn hardhat run scripts/deploy.js --network localhost


//to compile use
yarn hardhat compile